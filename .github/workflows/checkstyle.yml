name: Checkstyle

on: [push, pull_request]

jobs:
  checkstyle:
    runs-on: ubuntu-latest
    steps:
      - name: Run Checkstyle
        run: |
          cd ~/java-project-71/app
          ./gradlew checkstyleMain
